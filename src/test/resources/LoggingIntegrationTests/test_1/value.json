{
    "formatters": {
        "formatter": {
            "format": "${timestamp} ${level} ${logger} [${thread}] ${function}(${file}:${line}): ${message}",
            "parameters": {
                "timestamp": {"format": "%Y-%m-%d %H:%M:%S", "seconds": 9}
            }
        }
    },
    "handlers": {
        "console-handler": {
            "type": "console",
            "stream": "stdout",
            "formatter": "formatter"
        },
        "file-handler": {
            "type": "file",
            "file": "log.txt",
            "size": 1024,
            "formatter": "formatter"
        }
    },
    "loggers": {
        "root": {
            "level": 6,
            "handlers": [
                "console-handler",
                "file-handler"
            ]
        }
    }
}
